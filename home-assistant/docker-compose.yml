services:
  home-assistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    environment:
      TZ: "Europe/Berlin"
    ports:
      - 8123:8123
    volumes:
      - home-assistant-config:/config

volumes:
  home-assistant-config:
